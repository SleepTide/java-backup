const config = {
    serverDirectory: "/root/server/",
    backupDirectory: "/root/server/backups",
    worldName: "world",
    backupTime: 90,
    backupOnStart: true,
    debugging: true
};

export default config;
